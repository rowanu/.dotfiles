# General shortcuts
alias l='ls'
alias ll='ls -al'

# Tool shortcuts
alias rg='rg --hidden'
alias vim='nvim'

# Git (definitions live in ~/.dotfiles/.gitconfig [alias])
alias g='git'

# Utilities
alias simplehttpserver='python3 -m http.server'
alias rsyncssh='rsync --archive --compress --progress --rsh=ssh'
